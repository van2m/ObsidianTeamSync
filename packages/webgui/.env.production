# Production environment variables for WebGUI
# Same-origin deployment: leave empty (API uses relative path /api)
# Cross-origin deployment: set to server URL
# VITE_API_BASE_URL=https://your-cloudrun-domain.com/api
